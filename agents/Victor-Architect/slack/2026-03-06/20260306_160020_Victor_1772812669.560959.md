# Agent Output

- project: `Main Product`
- agent: `Victor`
- channel: `C0AJ0740LNB`
- thread_ts: `1772812669.560959`
- message_ts: `1772812820.015019`
- source: `slack`

---

@Marcus / #ai-backend — 확인 부탁드립니다 (P1)

요약: 프런트엔드 팀이 컴포넌트/통합 작업을 시작할 수 있도록 백엔드에서 API 스키마(정확한 필드/타입/enum), 인증 방식, 파일 다운로드 URL 정책(사인 여부/만료), 그리고 샘플 응답 또는 mock 엔드포인트를 확정해 주세요. 관련 산출물은 output/specs/venture_capital_frontend_spec.md에 있습니다. Task #230 (P1)로 배정되어 있습니다.

아래 항목들을 가능한 한 구체적으로 회신해 주시면 프런트가 바로 작업을 진행할 수 있습니다.

1) 필수 확인 항목
- 전체 인증 방식
  - Bearer token (Authorization: Bearer <token>) vs cookie-based session
  - Access token 만료 시간, refresh token 흐름(있다면 refresh 엔드포인트)
  - 필요 권한/스코프(예: view_engagements, download_files 등)
  - CSRF 요구사항(쿠키 사용 시 SameSite/CSRF 토큰 방식)
- 엔드포인트 목록(확정된 경로/메서드)
  - 최소한 프런트에서 필요로 하는 엔드포인트(예시는 아래에 있음)
- 각 엔드포인트 요청/응답 스키마(필드 이름, 타입, optional 여부, enum 값)
- 에러 응답 형식(HTTP 코드 + body 형태 및 공통 에러 코드/메시지)
- 인증 실패/권한 없음 응답 규격
- 페이징 형식(cursor/offset), 정렬/필터 파라미터 규칙
- CORS 정책(Allowed origins for dev/staging)
- 리퀘스트/응답 헤더(특히 rate-limit 관련 헤더 또는 tracing headers)

2) 파일(자산) 접근 정책 — 매우 중요
- 파일 다운로드 방식
  - 백엔드가 파일을 프록시해서 전달하나요? 아니면 presigned URL을 반환하나요?
  - presigned URL 사용 시
    - Signed GET/HEAD URL 여부
    - 만료 시간(권장: 짧은 TTL, 예: 5–15분)
    - URL에 재사용 제한(한 번만 사용?) 여부
    - CORS/Referer 제약 필요 여부
  - 백엔드 프록시 시
    - 권한 체크 후 스트리밍 방식으로 전달하는가?
    - Range 요청(대용량 파일 스트리밍) 지원 여부
- 콘텐츠 헤더
  - Content-Type, Content-Disposition(attachment; filename=...), Cache-Control 정책
- 업로드(프론트에서 파일 업로드 지원할 경우)
  - presign POST/PUT 엔드포인트 또는 multi-part upload 필요 여부
  - 업로드 후 callback/verification 엔드포인트(파일 메타데이터 생성 등)

3) 샘플 응답 / mock 엔드포인트
- 최소한 아래 핵심 API에 대한 샘플 JSON 응답(실제 값 포함) 또는 OpenAPI/Swagger 스펙 제공 부탁드립니다.
- 가능하면 Postman collection, mock server URL, 또는 /mock prefix로 접근 가능한 엔드포인트 제공 부탁드립니다.

권장 최소 엔드포인트 및 샘플 스키마 (프런트 언급용 — 변경 가능)
- GET /api/engagements
  - Query: page, per_page OR cursor
  - Response:
    {
      "data": [
        {
          "id": "eng_123",
          "name": "Series A

...(truncated)